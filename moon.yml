tasks:
  prettier-check:
    args:
      - "'!./projects/**/*'"
  prettier:
    args:
      - "'!./projects/**/*'"
  doctor:
    command:
      - doctor
      - .
    platform: node
  yarn-constraints:
    command:
      - yarn
      - constraints
    platform: node

tags:
  - prettier
  - typescript
