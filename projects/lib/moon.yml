tasks:
  test:
    command: node --test --test-reporter=spec --import tsx '**/*.test.ts'
  testWatch:
    command: node --test --watch --import tsx '**/*.test.ts'
    local: true

tags:
  - prettier
  - typescript
